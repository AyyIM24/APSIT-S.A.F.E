server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/apsit_safe_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:aYYU@3102}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=uploads

# Cloudinary (cloud image storage for production)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

jwt.secret=${JWT_SECRET:ApsitSafeDefaultDevKeyPleaseChangeInProduction2026!@#$%}
jwt.expiration=86400000

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# CORS
cors.allowed.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:3001}

# QR Code
app.qr.base-url=${QR_BASE_URL:http://localhost:3000}

# Google OAuth 2.0
google.client.id=${GOOGLE_CLIENT_ID:991700331879-hkoshiv02th660sqffa0sjlpv55pifqq.apps.googleusercontent.com}
apsit.allowed.domain=apsit.edu.in

# Email / SMTP (Gmail)
app.mail.enabled=${MAIL_ENABLED:true}
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USER:muqadam.ayyan242@gmail.com}
spring.mail.password=${MAIL_PASS:brdx ysjs zjmf pxfd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000